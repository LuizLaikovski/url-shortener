generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Urls {
  id       Int    @id @default(autoincrement())
  original String
  short    String @unique

  @@map("urls")
}
